# This is shelll config for no ineractive shell, like emacs compile mode.

   [[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" #

export PATH=$PATH:/usr/local/sbin
export PATH=$PATH:/usr/local/Cellar/imagemagick/6.8.7-7/bin/
   
